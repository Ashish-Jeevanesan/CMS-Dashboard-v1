export const environment = {
  production: true,
  // These will be replaced during build process with actual values
  // Use Angular's file replacement feature in angular.json for different environments
  supabaseUrl: '${SUPABASE_URL}', // Replace with actual URL during build
  supabaseKey: '${SUPABASE_ANON_KEY}' // Replace with actual key during build
};